<%= form_for [@user, @items.new], remote: true do |f| %>
  <div class="form-group">
    <%= f.label :Item %>
    <%= f.text_field :name, class: 'form-control', placeholder: 'Add a new todo item.' %>
    <%= f.submit 'Save', class: 'btn btn-success blue' %>
  </div>
<% end %>
